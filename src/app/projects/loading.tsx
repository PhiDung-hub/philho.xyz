import { SectionContainer } from '~/components';

export default function Projects() {
  return (
    <SectionContainer className="h-screen font-mono text-3xl md:text-4xl text-center animate-text-bounce flex flex-col items-center justify-center">
      Loading ...
    </SectionContainer>
  );
}
